name: "BundleScenes"
main: "gg.bundlegroup.bundlescenes.plugin.Main"
version: "${version}"
api-version: "1.21.8"
dependencies:
  server:
    WorldEdit:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      required: false
